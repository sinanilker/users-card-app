import React from 'react'
import PropTypes from 'prop-types'

const Header = () => {
    return (
        <div>Header</div>
    )
}

// Header.defaultProps = {
//     logoTitle: "Header",
// }

export default Header